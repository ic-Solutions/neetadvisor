compress_html:
  ignore:
    envs: [development]

# Disable compression in development
sass:
  style: expanded

# Enable incremental builds
incremental: true

# Disable plugins that slow down development
plugins: []