{
  "scripts": {
    "dev:config": "jekyll serve --config _config-dev.yml --livereload --livereload-port 3000",
    "dev": "jekyll serve --livereload --livereload-port 3000",
    "build": "jekyll build"
  }
}